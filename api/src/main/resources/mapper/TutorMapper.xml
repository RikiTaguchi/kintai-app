<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.api.mapper.TutorMapper">

    <select id="selectAll" resultType="com.example.api.mapper.entity.TutorEntity">
        select
            t.id,
            a.id as accountId,
            a.login_id,
            a.password,
            c.id as classroomId,
            c.name as classroomName,
            c.classroom_number,
            t.first_name,
            t.last_name,
            t.tutor_number,
            t.terminated,
            t.termination_date
        from
            tutors as t
        left join
            accounts as a on a.id = t.account_id
        left join
            classrooms as c on c.id = t.classroom_id
        where
            c.id = #{classroomId}
    </select>

    <select id="select" resultType="com.example.api.mapper.entity.TutorEntity">
        select
            t.id,
            a.id as accountId,
            a.login_id,
            a.password,
            c.id as classroomId,
            c.name as classroomName,
            c.classroom_number,
            t.first_name,
            t.last_name,
            t.tutor_number,
            t.terminated,
            t.termination_date
        from
            tutors as t
        left join
            accounts as a on a.id = t.account_id
        left join
            classrooms as c on c.id = t.classroom_id
        where
            t.id = #{tutorId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tutors
            (account_id, classroom_id, tutor_number, first_name, last_name)
        values
            (#{accountId}, #{classroomId}, #{tutorNumber}, #{firstName}, #{lastName})
    </insert>

    <update id="update">
        update
            tutors
        set
            tutor_number = #{tutorNumber},
            first_name = #{firstName},
            last_name = #{lastName},
            terminated = #{terminated},
            termination_date = #{terminationDate}
        where
            id = #{id}
    </update>

</mapper>
